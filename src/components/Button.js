import React from 'react';

const Button = () => {
	return (
		<div className="btn_wrapper">
			<button type="submit" className="btn main_btn">
				submit
			</button>
		</div>
	);
};

export default Button;
